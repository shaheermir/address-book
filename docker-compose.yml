version: '3.7'
services:
  address-book:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: address-book:dev
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
